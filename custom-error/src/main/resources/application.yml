spring:
  security:
    oauth2:
      client:
        registration:
          github:
            client-id: ${SB_GITHUB_ID}
            client-secret: ${SB_GITHUB_SECRET}
          google:
            client-id: your-google-client-id
            client-secret: your-google-client-secret
# logging:
#  level:
#    org.springframework.security: DEBUG
